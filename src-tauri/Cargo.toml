[package]
name = "feditile"
version = "0.0.0"
description = "FediTile desktop app"
authors = ["FediTile contributors"]
edition = "2021"

[lib]
name = "feditile_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
url = "2"
